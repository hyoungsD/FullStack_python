# range() : 범위

# range([start], stop, [step])
# start, step 생략가능
# start의 기본값 0
# step의 기본값 1
# 범위를 생성할때 stop값은 포함되지 않음

range(10)   # start, step 생략, 0~9까지의 범위 생성
range(1, 10)    # step 생략, 1~9까지의 범위 생성

